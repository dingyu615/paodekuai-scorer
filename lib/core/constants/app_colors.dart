import 'package:flutter/material.dart';

class AppColors {
  // Light theme base
  static const background = Color(0xFFF2F3F5);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceLight = Color(0xFFE8EAED);
  static const cardBorder = Color(0xFFE0E2E6);

  // Primary - blue
  static const primary = Color(0xFF4F5EF7);
  static const primaryDark = Color(0xFF3D4FC4);

  // Accent - gold
  static const gold = Color(0xFFC8963E);
  static const goldLight = Color(0xFFD4A853);
  static const silver = Color(0xFFA0A0A0);
  static const bronze = Color(0xFFB87333);

  // Text
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF);

  // Score
  static const positiveScore = Color(0xFF16A34A);
  static const negativeScore = Color(0xFFDC2626);

  // Player colors
  static const playerColors = [
    Color(0xFF4F5EF7),
    Color(0xFF16A34A),
    Color(0xFFD97706),
    Color(0xFFDC2626),
    Color(0xFF7C3AED),
    Color(0xFFDB2777),
  ];

  static const success = Color(0xFF16A34A);
  static const error = Color(0xFFDC2626);
  static const warning = Color(0xFFD97706);

  // Glass
  static const glassBg = Color(0x0A000000);
  static const glassBorder = Color(0x1A000000);
}
